import {TOTALS} from '../actions'

const initialState = {
  calories: 0,
  water: 0,
  clifbar: 0,
  gu: 0,
  pbj: 0,
  balls: 0
}

function totals (state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default totals
